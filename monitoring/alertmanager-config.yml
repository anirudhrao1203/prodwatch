route:
  receiver: "default-receiver"
  group_by: ["alertname"]
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: "default-receiver"
    webhook_configs:
      - url: "http://localhost:5001/"
        send_resolved: true
